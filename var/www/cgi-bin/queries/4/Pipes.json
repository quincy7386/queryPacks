{
    "device_filter": {
        "device_id": [
            DEVICE_ID
        ]
    },
    "name": "Pipes",
    "notify_on_finish": "false",
    "sql": "select pi.pid,pr.name as process,pi.name as pipe_name,instances,pi.flags from pipes as pi join processes as pr using(pid);"
}
